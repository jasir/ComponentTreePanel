{define #class-title}
    {default class=>'Yoy forgot to definee parameter class'}
    {php $exploded = explode('\\', $class)}
    {php $className = end($exploded)}
    <code title="{$class}">{$className}</code>
{/define}